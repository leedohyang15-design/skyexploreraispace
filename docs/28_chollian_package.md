# 천리안 1호 쇼 — Studio 실행 패키지

상영용 5분 쇼 "천리안 1호 — 여정과 마감"을 **처음 보는 사람이 그대로 따라 돌릴 수 있게** 정리한 문서.

## 1. 구성물

| 파일 | 역할 | 위치 |
|---|---|---|
| `scripts/golden/SHOW_chollian.py` | **쇼 본편** (5분 01초) | Studio 로 가져온다 |
| `scripts/study/make_chollian_model.py` | **위성 모델 생성기** — 유저 폴더에 모델 파일을 쓴다 | Studio 로 가져온다 (한 번만 실행) |
| `docs/27_chollian_narration.md` | 나레이션 대본 + 큐시트 (음악 큐 6개) | 오퍼레이터가 읽는다 |
| 이 문서 | 설치·실행·점검 | |

⚠️ **모델 파일(`chollian.osg`)은 저장소에 없다.** 생성기를 한 번 돌리면 만들어진다.
Studio 파이썬이 파일을 쓸 수 있다는 게 확인돼 있어(2026-08-12 실측) 손으로 복사할 일이 없다.

## 2. 설치

### 2-1. 모델 만들기 (최초 1회)

1. Studio 에 `make_chollian_model.py` 를 드래그앤드롭 → 데이터베이스 스크립트 → 더블클릭 실행
2. 로그에서 이 두 줄을 확인한다:
   ```
   ✓ chollian.osg  ...바이트
   ✅ chollian.osg  status=Loaded  radius=5.27...
   ```
3. 파일은 **유저 폴더**에 생긴다: `D:/SkyExplorer-Data/user/chollian.osg`

> 폴더 경로가 다른 설치라면 스크립트가 로그 첫머리에 실제 경로를 찍는다
> (`Configuration.configuration().localUserFolder`). 그 경로를 믿으면 된다.

⚠️⚠️ **경로 규약이 두 가지라 헷갈리기 쉽다:**
- **우리가 넣는 자산**(모델·이미지·텍스처) = **유저 폴더** 기준. 파일명만 써도 되고 절대경로도 된다
- **Studio 기본 자산**(`..\data\scene\astronomy\...`) = 그 **유저 폴더의 부모** 기준
  (`D:\SkyExplorer-Data\data\`). 블랙홀 모델 경로가 그래서 `..\data\` 로 시작한다

### 2-2. 쇼 넣기

`SHOW_chollian.py` 를 같은 방법으로 Studio 에 가져온다. 실행은 더블클릭.

## 3. 상영 전 점검표

⚠️ **1~3번은 1차 돔 재생에서 실제로 깨졌던 것들이다. 여기서 걸리면 그 뒤는 볼 필요가 없다.**

| # | 확인 | 통과 기준 |
|---|---|---|
| 1 | **0:51 우주 자막** | 지구가 나온 뒤에도 **글자가 뜬다** ← v1 은 여기부터 4분간 자막이 하나도 없었다 |
| 2 | **지구 색** | 파란 바다·갈색 대륙이 보인다 ← v1 은 회색 공이었다 |
| 3 | **2:50 지구 클로즈업** | 지구가 **화면을 채운다** ← v1 은 텅 빈 우주에 궤도선만 떠 있었다 |
| 4 | 모델 파일 존재 | 유저 폴더에 `chollian.osg` 가 있다 |
| 5 | 0:40 지구 등장 | 북극 위에서 지구 원반이 보인다. **알아볼 만한 크기여야 한다** |
| 6 | 0:58 노란 궤도선 | 지구 둘레에 노란 원이 뜬다 |
| 7 | **1:12 위성 등장** | 궤도 위에 흰 위성이 보인다 |
| 8 | 1:43~ 범례 | 화면 위쪽에 노랑·파랑·초록 이름표 세 줄이 뜬다 |
| 9 | **1:56 시간가속** | 위성 **세 개가 각자 속도로 돈다**(ISS 빠름 / GPS 중간 / 천리안 느림) + 지구가 자전한다 |
| 10 | 카메라 기울기 | 막2 동안 북극 위에서 비스듬한 각도로 **천천히** 기운다 |
| 11 | 2:53 구름 | 구름이 서서히 덮인다 |
| 12 | **4:02 무덤 궤도** | 위성이 나선을 그리며 눈에 띄게 위로 올라간다 |
| 13 | 4:23 소등 | 위성이 어두워진다 |
| 14 | 전체 길이 | 5분 01초 (±5초) |
| 15 | 음향 | **스크립트는 무음** — 나레이션·음악이 준비돼 있다 |

## 4. 실패 시 대처

**1번(우주 장면에서 자막이 안 뜬다) — v1 을 죽인 버그**

- 프레임마다 **자막 규칙이 반대**다: **지상 = `setSize(0.052)` + `setDistance(1.0)` /
  우주 = 크기를 만지지 말고 `setDistance(20)`**. 우주에서 `setSize` 를 부르면 자막이 **사라진다**
- ⚠️ 크기를 되돌리는 API 가 없다 — 지상에서 쓰던 자막 객체의 거리만 20 으로 바꿔도 안 보인다.
  **크기를 한 번도 안 건드린 새 슬롯**으로 갈아타야 한다
- 이 규칙은 린터에 들어가 있다. 쇼를 고쳤다면 린터를 먼저 돌려 볼 것

**7번(위성이 안 보인다)**

세 번 다른 이유로 겪었다. **이 순서대로** 의심한다 — 배율은 맨 마지막이다:

1. **구도.** 비스듬한 각도(B=35)에서는 정지궤도가 화면 밖으로 밀린다. 이 쇼는 **북극 위 B=88** 에서
   시작한다(`B_TOP`). 링이 원형이라 안 잘린다
2. **거리.** `R_TOP = 10`. v1 은 20 이었는데 그때 지구 각지름이 5.7° = 점이었다
   ("이게 어떻게 위에서 내려다보는 구도냐"). 20 으로 되돌리지 마라
3. **렌더.** 지구가 안 보이면 `setTerrainIntensity` 를 의심한다 — 0 이면 **가까이 갈수록 사라진다**
4. **모델 로드.** 로그에 `⚠️ 모델 로드 실패` 가 있으면 2-1 을 다시 한다
5. **배율.** `SCALE_ORBIT = 5.0e5` (위성 반지름 2,636 km). R 을 바꾸면 **배율도 같이** 바꿔야
   겉보기 크기가 유지된다(거리 절반 → 배율 절반)

**모델 로드가 실패한다**
- 유저 폴더 경로를 로그에서 확인한다. 생성기가 찍는 경로가 진실이다
- 로그가 `Loading` 에서 멈춘 것처럼 보여도 실패가 아닐 수 있다 — 이 쇼는 `Loaded` 가 뜰 때까지
  최대 12초 폴링한다. 12초를 넘겼다면 진짜 실패다

**색이 흰색이다 — 정상이다**
- 현재 모델은 **흰-은색**이다. 엔진 조명이 켜져 있어 모델의 정점 색이 무시된다(재질 미적용).
- 실제 위성도 흰 단열재와 은색 몸체라 틀린 그림은 아니다. 금색 태양전지판은 재질 문법을 확인한 뒤 추가 예정
- 색이 들어와도 **쇼 코드는 바뀌지 않는다** — 모델 파일만 갈아 끼우면 된다

**쇼가 중간에 멈춘다**
- 막마다 `try/except` 로 감싸 놨다. 로그의 `막N 오류:` 를 보면 어디서 죽었는지 나온다
- 그 막만 건너뛰고 다음 막이 이어지므로 상영 자체는 계속된다

**앞 쇼의 마지막 화면이 번쩍인다**
- 이 쇼는 암전을 `reset` 보다 먼저 걸고 클램프로 눌러 둔다. 그래도 보인다면
  Studio 에서 앞 쇼를 완전히 정지한 뒤 실행한다

## 5. 조정하고 싶을 때

| 바꾸고 싶은 것 | 건드릴 곳 | 주의 |
|---|---|---|
| 위성 크기 | `SCALE_ORBIT` | 5e5. **R 을 바꾸면 배율도 같은 비율로** — 안 그러면 겉보기 크기가 달라진다 |
| 궤도 조망 각도·거리 | `B_TOP`(88), `R_TOP`(10) | 각도는 건드리지 마라 — 비스듬하면 링이 잘린다. 거리는 8~12 사이가 안전 |
| 지구 클로즈업 | `B_NEAR`, `R_NEAR` | 프레이밍이 바뀌므로 암전 안에서만 |
| 무덤 궤도 높이 | `GRAVE_MM`, `GRAVE_R` | 둘을 같이 바꿔야 궤도선과 위성이 맞는다 |
| 자막 속도 | `say()` 의 자동 계산 | `2초 + 글자당 0.1초`. 숫자를 직접 박지 마라 |
| 관측지·시각 | 막0 의 `Place2D.setPosition` / `setDateTime` | **시각은 UTC**. 청주 22시 = 13:00 UTC |

## 6. 알려진 제약

- **소리**: 스크립트로 못 낸다(엔진 확정). 관 음향으로 입힌다
- **축척 과장 2곳**: 위성 크기(1:21), 무덤 궤도 높이(4:16). 둘 다 나레이션에서 고지한다 — 빼지 말 것
- **모델 색**: 흰-은색 (위 참조)
- **검증 상태**: **v1 은 돔에서 돌려 네 군데가 깨진 것을 확인했고, v2 는 그걸 고쳤으나 아직 안 돌려 봤다.**
  첫 상영 전에 반드시 리허설한다. 확인 후 스크립트 첫머리의 `# 검증:` 줄을 갱신하고
  `tools/ledger.py` 로 원장에 올린다
