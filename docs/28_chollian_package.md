# 천리안 1호 쇼 — Studio 실행 패키지

상영용 5분 쇼(실측 **5분 01초**) "지구를 바라보는 하나의 눈 — 천리안 1호의 11년"을
**처음 보는 사람이 그대로 따라 돌릴 수 있게** 정리한 문서.

> ⚠️⚠️ **사실 정정(2026-08-13)**: 전 판은 "16년 / 2025년 12월 종료"로 썼는데 **틀렸다.**
> 천리안 1호는 **2010-06-27 발사 → 2021년 4월 폐기궤도**, 약 **11년** 운용이다.
> 설계 수명은 7년(2017년까지). 모든 문서를 이 값으로 고쳤다.

## 1. 구성물

| 파일 | 역할 | 위치 |
|---|---|---|
| `scripts/golden/SHOW_chollian.py` | **쇼 본편** (5분 01초) | Studio 로 가져온다 |
| `scripts/study/make_chollian_model.py` | **위성 모델 생성기** | Studio 로 가져온다 (한 번만 실행) |
| `scripts/study/make_orbit_ring.py` | **궤도선 고리 생성기** — 금색·회색·청록 3종 | Studio 로 가져온다 (한 번만 실행) |
| `docs/27_chollian_narration.md` | 나레이션 대본 + 큐시트 (음악 큐 6개) | 오퍼레이터가 읽는다 |
| 이 문서 | 설치·실행·점검 | |

⚠️ **모델 파일은 저장소에 없다** — `chollian.osg`(위성) · `ring_gold.osg`/`ring_gray.osg`(궤도선).
**생성기 두 개를 각각 한 번씩** 돌리면 만들어진다.
Studio 파이썬이 파일을 쓸 수 있다는 게 확인돼 있어(2026-08-12 실측) 손으로 복사할 일이 없다.

## 2. 설치

### 2-1. 모델 만들기 (최초 1회)

1. Studio 에 `make_chollian_model.py` 를 드래그앤드롭 → 데이터베이스 스크립트 → 더블클릭 실행
2. 로그에서 이 두 줄을 확인한다:
   ```
   ✓ chollian.osg  ...바이트
   ✅ chollian.osg  status=Loaded  radius=5.27...
   ```
3. **이어서 `make_orbit_ring.py` 도 같은 방법으로 한 번 실행**한다. 로그에 세 줄이 나온다:
   ```
   ✓ ring_gold.osg  ...바이트
   ✓ ring_gray.osg  ...바이트
   ✓ ring_cyan.osg  ...바이트     ← 프로브 전용(쇼는 안 씀)
   ```
4. 파일은 전부 **유저 폴더**에 생긴다: `D:/SkyExplorer-Data/user/`

> 폴더 경로가 다른 설치라면 스크립트가 로그 첫머리에 실제 경로를 찍는다
> (`Configuration.configuration().localUserFolder`). 그 경로를 믿으면 된다.

⚠️⚠️ **경로 규약이 두 가지라 헷갈리기 쉽다:**
- **우리가 넣는 자산**(모델·이미지·텍스처) = **유저 폴더** 기준. 파일명만 써도 되고 절대경로도 된다
- **Studio 기본 자산**(`..\data\scene\astronomy\...`) = 그 **유저 폴더의 부모** 기준
  (`D:\SkyExplorer-Data\data\`). 블랙홀 모델 경로가 그래서 `..\data\` 로 시작한다

### 2-2. 쇼 넣기

`SHOW_chollian.py` 를 같은 방법으로 Studio 에 가져온다. 실행은 더블클릭.

## 3. 상영 전 점검표

⚠️ **0~5번은 돔 재생에서 실제로 깨졌던 것들이거나 v9 에서 처음 쓰는 것들이다.
여기서 걸리면 그 뒤는 볼 필요가 없다.**

| # | 확인 | 통과 기준 |
|---|---|---|
| 0 | **모델 두 개를 다 만들었나** | 유저 폴더에 `chollian.osg` + `ring_gold.osg` + `ring_gray.osg`. **생성기 두 개를 각각 한 번씩** 돌린다 |
| 1 | **새 모델의 솔라세일** | 태양전지판 **반대편에 긴 막대 + 네모 반사판**이 보인다(실물의 특징). 없으면 모델 생성기를 다시 돌린 것이 아니다 |
| 2 | **0:21~0:41 하강 (v9 신규)** | 은하수 딥 스페이스에서 **한반도 상공까지 내려오는 게 보인다** |
| 3 | **0:53~1:15 상승 (v9 신규)** | 쿠루 상공에서 지구가 **작아지는 게 보인다**. 화면이 비거나 지표가 뭉개지면 `R_LAUNCH` 를 1.9~2.2 로 |
| 4 | **1:21~1:51 파수꾼** | 위성이 천천히 한 바퀴 돌고, **카메라가 위성 쪽으로 다가간다** |
| 5 | **2:14 돔 시계 (v9 신규·미검증)** | 시계가 화면에 뜨고 시간가속에 맞춰 **바늘이 돈다**. 안 보이면 `SHOW_CLOCK = False` |
| 6 | **2:27~3:15 지구 자전·낮밤** | 지구가 돌고(하늘이 도는 게 아니라) **낮밤이 두 번** 지나간다 |
| 7 | 2:54~3:05 계승 | 2A·2B 가 나타나 위성이 셋이 된다 |
| 8 | **3:12 / 3:19 궤도선** | 금색·회색이 **닫힌 원**이다(고리 모델이라 나선이 나올 수 없다) |
| 9 | **3:26~3:42 이탈 (핵심)** | 위성이 **금색 원에서 회색 원으로 건너가는 게** 보인다 |
| 10 | 3:50 소등 | 위성이 어두워지되 **화면에서 사라지지 않는다** |
| 11 | 3:50~4:04 줌아웃 | 지구가 **멀어지는 게 보인다** |
| 12 | **4:11 도시 불빛 (v9 신규)** | 한반도 밤면에 **호박색 도시 불빛**이 켜진다 |
| 13 | **4:40 지상 복귀 (v9 신규)** | 암전 뒤 **청주 밤하늘**이 뜬다. 검은 화면이면 `ground_night()` 의 reset 을 확인 |
| 14 | **지구 색** | 파란 바다·갈색 대륙이 보인다 |
| 15 | **위성 색** | 금색 본체 · 남색 전지판 · 흰 접시 (흰색뿐이면 생성기를 다시 돌린다) |
| 16 | 전체 길이 | 5분 01초 (±5초) |
| 17 | **자막** | 지금은 **꺼져 있다**(`SHOW_TEXT = False`). 켜려면 그 한 줄만 `True` |

## 4. 실패 시 대처

**우주 장면에서 자막이 안 뜬다 — 두 번 연속 이걸로 깨졌다**

- 프레임마다 **자막 규칙이 반대**다: **지상 = `setSize(0.052)` + `setDistance(1.0)` /
  우주 = 크기를 만지지 말고 `setDistance(20)`**
- ⚠️⚠️ **핵심은 '`setSize` 를 안 부르는 것'이 아니라 '슬롯을 갈아타는 것'이다.**
  크기를 되돌리는 API 가 없어서, 지상에서 크기를 걸어 둔 슬롯을 우주에서 다시 잡으면
  **호출을 안 해도 자막이 사라진다**(v3 가 정확히 이걸로 죽었다 — 범례는 새 슬롯이라 떴고
  본 자막만 안 떴다)
- 이 쇼는 `SLOT_GROUND = 1` / `SLOT_SPACE = 5` 로 고정돼 있다. **이 둘을 섞지 마라**
- 린터에 규칙이 두 개 들어가 있다(같은 객체 / 슬롯을 인자로 받는 헬퍼). 고쳤으면 먼저 돌려 볼 것

**지구가 안 돌거나, 위성이 튄다 — 프레임 문제다**

이 쇼는 **프레임 세 개**를 구분해서 쓴다. 섞으면 바로 깨진다:
- **관성(`ip` = EquatorialJ2000)** = 카메라의 집. 여기 있어야 **지구가 도는 게 보인다.**
  FadeTo 도킹 프레임에 그냥 두면 카메라가 자전을 따라 돌아 **지구가 멈추고 하늘이 돈다**
- ⚠️ 프레임 이동은 **반드시 두 줄** — `setPositionLBR(..., 포트)` **+ `setOrientationSmoothXYZR(...)`**.
  둘째 줄을 빼면 카메라가 대상 없는 쪽을 봐서 **화면이 텅 빈다**(실제로 그랬다). 헬퍼 `fly()` 를 쓸 것
- **동기(`sp` = EquatorialSynchronous)** = 위성의 집. 이 프레임의 경도 = 지구 경도라
  **128.2 를 넣으면 한반도 위에 자동으로 붙는다.** 위성을 손으로 밀지 마라 — 시간가속과 겹쳐 **급발진**한다

**위성이 안 보인다**

**이 순서대로** 의심한다 — 배율은 맨 마지막이다:

1. **켜져 있나.** Intro·Scene 1 에서는 **일부러 꺼 둔다**(아직 안 만난 상태). Scene 2 부터 켜진다
2. **거리.** Scene 2 는 R 10 → 8.3. 위성(6.611)보다 **바깥**이어야 지구 앞에 놓인다
3. **렌더.** 지구가 안 보이면 `setTerrainIntensity` — 0 이면 **가까이 갈수록 사라진다**
4. **모델 로드.** 로그에 `⚠️ 모델 로드 실패` 가 있으면 2-1 을 다시 한다
5. **배율.** `SCALE_SAT = 1.0e6` (반지름 5,270 km)

**궤도선이 안 보인다 / 옆으로 서 있다**
- ⚠️ **먼저 `make_orbit_ring.py` 를 돌렸는지 확인한다.** 고리 파일이 없으면 아무것도 안 그려진다
  (로그에 `모델 로드 실패` 가 찍힌다)
- 고리가 **원이 아니라 '선'으로 서 있으면** `RING_HPR` 한 줄만 바꾸면 된다.
  ✅ 확정값은 `Vec(0,0,0)`(적도면에 누움). `(0,90,0)` 은 옆으로 선다
- ⚠️⚠️ **`OrbitalPlace` 로 되돌리지 마라** — 이 빌드에서 **늘 나선**이다(판별 프로브로 확정).
  검증된 예제 코드를 그대로 돌려도 나선이었다. 클래스 자체가 안 되는 것이다
- 궤도선은 **Scene 4(멀리서)에서만** 켠다. 금색 = 11년을 돈 자리 / 회색 = 폐기궤도

**색이 안 나온다**
- ✅ **해결됐다** — `.osg` 의 `Geometry` 안에 `StateSet{ Material{ diffuseColor } }` 를 넣으면 나온다.
  `ColorArray` 만으로는 안 먹는다(조명이 켜져 있어 재질 기본 흰색이 이긴다).
- `make_chollian_model.py` 를 **다시 한 번 돌리면** `chollian.osg` 가 색 판으로 덮어써진다.
  쇼 코드는 안 바뀐다. 흰색으로 나오면 생성기를 안 돌린 것이다

**모델 로드가 실패한다**
- 유저 폴더 경로를 로그에서 확인한다. 생성기가 찍는 경로가 진실이다
- 로그가 `Loading` 에서 멈춘 것처럼 보여도 실패가 아닐 수 있다 — 이 쇼는 `Loaded` 가 뜰 때까지
  최대 12초 폴링한다. 12초를 넘겼다면 진짜 실패다

**시계가 안 보인다 (Scene 3)**
- 돔 시계는 지상 장면에서 검증됐고 **우주 프레임에서의 `setDistance` 값은 미검증**이다.
  안 보이면 `SHOW_CLOCK = False` 로 끄면 된다 — 길이·타이밍은 안 바뀐다
- 살려 보고 싶으면 `clock_hud()` 의 `setDistance(1.0)` 을 20 으로 바꿔 A/B 해 볼 것

**쇼가 중간에 멈춘다**
- 장면마다 `try/except` 로 감싸 놨다. 로그의 `Scene N 오류:` 를 보면 어디서 죽었는지 나온다
- 그 장면만 건너뛰고 다음이 이어지므로 상영 자체는 계속된다

**앞 쇼의 마지막 화면이 번쩍인다**
- 이 쇼는 암전을 `reset` 보다 먼저 걸고 클램프로 눌러 둔다. 그래도 보인다면
  Studio 에서 앞 쇼를 완전히 정지한 뒤 실행한다

## 5. 조정하고 싶을 때

| 바꾸고 싶은 것 | 건드릴 곳 | 주의 |
|---|---|---|
| 위성 크기 | `SCALE_SAT` | 1e6(반지름 5,270km). **R 을 바꾸면 배율도 같은 비율로** — 안 그러면 겉보기 크기가 달라진다 |
| **이륙 고도(1순위 손잡이)** | `R_LAUNCH`(1.6) | v9 신규·미검증. 지표가 뭉개지거나 화면이 비면 **1.9~2.2** 로 올린다 |
| Intro 하강 | `R_DEEP`(26) → `R_INTRO_END`(3.4) | 시작이 너무 허전하면 `R_DEEP` 을 18~20 으로 |
| Scene 2 파수꾼 | `R_WATCH_A`(10) → `R_WATCH_B`(8.3) | ⚠️ 7.6 은 전지판 하나가 돔을 다 덮었다(실측). 8.3 아래로 내리지 말 것 |
| 낮밤 장면 | `B_TOGETHER`(40), `R_TOGETHER`(9) | 그림자 ON 구간. **오블리크라야 낮밤 경계가 보인다**(북극 위면 안 보임) |
| 이탈·줌아웃 | `R_BACK`(13) → `R_OUT`(20) | **각도(`B_TOP`=88)는 건드리지 마라** — 프레이밍이 깨진 건 세 번 다 각도였다 |
| Outro 도시 불빛 | `B_CITY`(28), `R_CITY`(4.2), `R_LAND`(2.0) | 불빛이 안 보이면 시각을 확인 — 한반도가 **밤**이어야 한다(13:00 UTC = 22시 KST) |
| 무덤 궤도 높이 | `GRAVE_R`(10.5) | 고리 반지름과 위성 도착점이 **이 값 하나로** 같이 정해진다(따로 맞출 필요 없다) |
| 궤도선 굵기 | `make_orbit_ring.py` 의 `BAND`(0.012) | 반지름 대비 비율. 안 보이면 0.02~0.03 |
| 자막 속도 | `say()` 의 자동 계산 | `2초 + 글자당 0.1초`. 숫자를 직접 박지 마라 |
| 관측지·시각 | `ground_night()` 의 `Place2D.setPosition` / `setDateTime` | **시각은 UTC**. 청주 22시 = 13:00 UTC |

## 6. 알려진 제약

- **소리**: 스크립트로 못 낸다(엔진 확정). 관 음향으로 입힌다
- **축척 과장 3곳**: 위성 크기, 폐기궤도 높이(3:45 에서 고지), 2A·2B 경도(겹쳐 보여 벌림)
- ⚠️⚠️ **대본대로 못 하는 것 — 로켓**: 발사대·아리안 5호·화염은 이 빌드에서 못 그린다
  (지상 3D 자산이 없다). Scene 1 은 **로켓에 올라탄 시점의 상승**으로 대체했다.
  로켓을 실제로 보여주려면 **고리처럼 모델을 하나 더 구우면 된다**(가능하다)
- ⚠️ **Scene 3 의 '천리안 주위 360° 궤도 뷰'도 못 한다** — 이 엔진의 카메라는 지구 중심을
  기준으로만 돈다. **위성을 제자리에서 돌리는 것**으로 대체했다(효과는 거의 같고 위험은 0)
- **모델(2026-08-13 갱신, 35조각 1,020삼각형)**: 실물 사진과 대조해 다시 만들었다 —
  **솔라세일**(반대편 붐 끝 반사판, 전에 빠져 있던 실물의 특징) · 3장짜리 태양전지판 + 셀 격자 ·
  Ka 주반사판 + 보조 반사판 + S 대역 혼 · **관측기 두 대(MI 기상 · GOCI 해양)** ·
  원지점 엔진 + 자세제어 추력기 4기. 폭 **8.88 m**, modelRadius **5.25 m**(전과 같아 쇼 배율 유효)
- **v9 에서 처음 쓰는 것(미검증)**: Intro 하강 · 이륙 고도 R=1.6 · **돔 시계 HUD** ·
  Outro 도시 불빛 + 지상 복귀 · **새 모델**. 점검표 1~5·12·13번
- ✅✅ **궤도선 해결(2026-08-13)**: `OrbitalPlace` 는 이 빌드에서 **늘 나선**이라 버렸다
  (판별 프로브의 A 단계 = 검증된 예제 코드 그대로였는데 그것도 나선이었다).
  → **직접 구운 고리 모델**(`make_orbit_ring.py`)로 대체. 닫힌 원 + 위성이 고리 위에 얹히는 것까지 확인.
  ⚠️ **설치 항목이 하나 늘었다**: 모델 생성기를 **두 개** 돌려야 한다 —
  `make_chollian_model.py`(위성) · `make_orbit_ring.py`(궤도선 고리 3종)
- **검증 상태**: **돔 재생 5회 + 프로브 3회.** 프로브로 확인된 것 —
  관성/동기 프레임 · 동기 프레임 경도 128.2 의 위성 · 진짜 줌인 ·
  **`OrbitalPlace` 사망** · **고리 모델의 방향(0,0,0)과 축척**.
  ⚠️ **v9 구성으로 통짜 재생은 아직 안 해 봤다.** 첫 상영 전에 반드시 리허설한다.
  확인 후 스크립트 첫머리의 `# 검증:` 줄을 갱신하고 `tools/ledger.py` 로 원장에 올린다
