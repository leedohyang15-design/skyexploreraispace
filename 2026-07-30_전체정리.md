

---

## J. 쇼 제작에서 얻은 연출 규칙 (장편 4편 만들며 실측)

### 자막
- **기본 높이 `Vec(0, 12, 0)`** ← 사용자 확정 (옛 25는 천체와 겹침). 지상·행성 프레임 공통.
  · 지상: `setDistance(1.0)` + `setSize(0.052)` / 행성 프레임: `setDistance(20)` + **setSize 금지**
- ⚠️ **새 자막을 만들 땐 이전 자막을 반드시 끈다**(`setIntensity(0, Anim(0))`) — 안 끄면 옛 자막이 남아 **"위치가 안 바뀐 것처럼" 보인다**(실측 사고).
- 각 쇼 상단 `CAPTION_H` 상수 하나로 전체 조절.

### 암전
- **암전은 '프레임이 진짜 바뀔 때만'** — GoTo 비행·관측지 이동·FadeTo 는 **그 자체가 부드러운 전환**이라 암전을 덧씌우면 이야기가 끊긴다.
- 필요한 곳은 **행성/딥스카이 → 지상 복귀**(`reset(1)` 필수) 뿐.
- ⚠️⚠️ **`reset(1)` 은 GlobalIntensity 를 1.0 으로 되돌린다** → 한 번 암전을 걸어두는 걸론 부족.
  **클램프 루프**로 그 구간 내내 0을 다시 찍어 눌러야 재세팅이 안 보인다:
  `for _ in range(N): uni.setGlobalIntensity(0.0, Anim(0.0)); sleep(0.2)`
- **세팅 분산** — 무거운 호출(특히 `setDateTime` 날짜 점프) 사이에 `sleep(0.3~0.4)`. 몰아치면 화면이 뚝뚝 끊긴다.

### 딥스카이
- **확대 전엔 별자리 선을 끈다** — 성운에 선이 겹치면 너무 복잡.
- ⚠️ **메시에 이름 매칭 사고**: `data(NebulaType, "M1")` 이 **구상성단으로 이동**했다 →
  **NGC 번호 우선**(게성운 = `"NGC 1952"`) + **이름 후보를 순회하며 `action(GoTo) is not None` 인 것을 채택**하고 어느 이름이 쓰였는지 로그로 남길 것.
- **도착 판정에 R 절대값 금지** — 행성은 R≈4~5 지만 딥스카이는 도착해도 R 이 거대(1.1e13). **상대 변화율**로 판정.

### 행성·위성
- ⚠️⚠️ **위성 '공전'이 안 보이면 = 관성 프레임 전환 누락**. 도킹 프레임(EquatorialSynchronous)은 카메라가 행성 자전을 따라 돌아 **하늘이 도는 것처럼** 보인다.
  → `portId(EquatorialJ2000)` 로 전환(같은 L/B/R + `setOrientationSmoothXYZR` 시선정렬).
  → ⚠️ **전환은 '도착 직후'에** 해야 한다. 위성을 켠 뒤에 하면 **화면이 갑자기 바뀌어** 흐름이 끊긴다(실측 지적).
- **위성계는 풀백 필수**: 도킹 R≈5 에선 궤도가 화면 밖 → `setPositionR(p.z*3.5)` + 위성 `setScale(14)`.
- **가속 범위**: 갈릴레이 위성 주기 이오 1.77 / 유로파 3.55 / 가니메데 7.15 / 칼리스토 16.7일 → **+8일을 50초**가 적당.

### 연출 흐름
- **'변화'는 이동과 동시에** — 관측지를 옮긴 **뒤에** 밝기를 바꾸면 작위적. `GoTo` 트리거 직후 **긴 Anim(10~12초)** 으로 함께 걸어 이동하는 동안 변하게.
- **관측지 이동도 애니메이션** → 좌표가 멈출 때까지 폴링(안 하면 파리를 40.49/88.07 로 읽음).

---

## K. 완성된 쇼 (scripts/golden/)
| 파일 | 내용 | 길이 |
|---|---|---|
| `SHOW_where_are_we.py` | 우리는 어디에 있는가 (관측지→별자리→딥스카이→행성→귀환) | ~10분 |
| `SHOW_life_of_a_star.py` | 별의 일생 (색=온도 → 탄생 M42 → 죽음 고양이눈/게성운) | ~8분 |
| `SHOW_grand_tour.py` | 태양계 대여행 (화성→목성+위성→토성 고리→명왕성) | ~9분 |
| `SHOW_lost_night.py` | 사라진 밤하늘 (어둠에서 시작→도시로 가며 별이 사라짐) | ~7분 |
| `g1~g8` | 규칙별 단편 검증 예제 8개 | 각 1~3분 |
